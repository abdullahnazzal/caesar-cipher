def encrypt(text ,numeric):
    """
    """
    words=text.split()
    cipher=[]
    for word in words:
        pass
    return cipher


def decrypt(text ,numeric):
    return encrypt(text , -numeric)
